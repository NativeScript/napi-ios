# @nativescript/ios-v8

NativeScript runtime package for iOS, built with the V8 JavaScript engine.

## Build

From the repo root:

```sh
npm run build-ios -- --v8
```

This stages the package in `packages/ios-v8/dist/package` and produces the npm artifact in `packages/ios-v8/dist/`.
