# @nativescript/ios-hermes

NativeScript runtime package for iOS, built with the Hermes JavaScript engine.

## Build

From the repo root:

```sh
npm run build-ios -- --hermes
```

This stages the package in `packages/ios-hermes/dist/package` and produces the npm artifact in `packages/ios-hermes/dist/`.
