# @nativescript/ios

NativeScript runtime package for iOS.

## Build

From the repo root:

```sh
npm run build-ios
```

This stages the package in `packages/ios/dist/package` and produces the npm artifact in `packages/ios/dist/`.
