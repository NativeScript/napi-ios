# @nativescript/ios-jsc

NativeScript runtime package for iOS, built with the JavaScriptCore engine.

## Build

From the repo root:

```sh
npm run build-ios -- --jsc
```

This stages the package in `packages/ios-jsc/dist/package` and produces the npm artifact in `packages/ios-jsc/dist/`.
