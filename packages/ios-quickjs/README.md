# @nativescript/ios-quickjs

NativeScript runtime package for iOS, built with the QuickJS JavaScript engine.

## Build

From the repo root:

```sh
npm run build-ios -- --quickjs
```

This stages the package in `packages/ios-quickjs/dist/package` and produces the npm artifact in `packages/ios-quickjs/dist/`.
