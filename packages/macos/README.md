# @nativescript/macos

NativeScript runtime package for macOS.

## Build

From the repo root:

```sh
npm run build-macos
```

This stages the package in `packages/macos/dist/package` and produces the npm artifact in `packages/macos/dist/`.
