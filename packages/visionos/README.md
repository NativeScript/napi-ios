# @nativescript/visionos

NativeScript runtime package for visionOS.

## Build

From the repo root:

```sh
npm run build-visionos
```

This stages the package in `packages/visionos/dist/package` and produces the npm artifact in `packages/visionos/dist/`.
